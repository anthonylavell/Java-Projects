package leetcode.heap.main;

public class HeapMain {
    public static void main(String[] args) {
       /* int [] values = {8,11,12,15,9};
        int [] values2 = {10,6,7,13};
        MaxHeap maxHeap = new MaxHeap(values.length);
        MaxHeap maxHeap2 = new MaxHeap(values2.length);
        for(int i = 0; i < values.length; i++){
            maxHeap.insert(values[i]);
        }
        for(int i = 0; i < values2.length; i++){
            maxHeap2.insert(values2[i]);
        }
        System.out.println(Arrays.toString(maxHeap.display()));
       // maxHeap.remove();
       /* System.out.println(Arrays.toString(maxHeap.display()));
        maxHeap.remove();
        System.out.println(Arrays.toString(maxHeap.display()));
        maxHeap.remove();
        System.out.println(Arrays.toString(maxHeap.display()));*/
       /* System.out.println("-------------------------------------");
        System.out.println(Arrays.toString(maxHeap2.display()));
        System.out.println("-------------------------------------");
        MaxHeap maxHeap3 = MergeMaxHeap.Merge(maxHeap,maxHeap2);
        System.out.println(Arrays.toString(maxHeap3.display()) );*/
        System.out.println(36/10);
    }
}
